service:
  rf-system:
    id: rf-system-test
  rf-flow:
    id: rf-flow-test
spring:
  application:
    name: rf-flow
  datasource:
    url: jdbc:h2:mem:/rf_flow
    platform: h2
    driver-class-name: org.h2.Driver
    username: sofa
    password: 123456
    data: classpath:data-test.sql
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
com:
  alipay:
    sofa:
      rpc:
        registry-address: nacos://118.25.44.154:8999
logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    org.hibernate.event.internal.DefaultPersistEventListener: debug
    org.flowable.task.service.impl.persistence.entity: debug
    com.realfinance: debug